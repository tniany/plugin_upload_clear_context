name: astrbot_plugin_clear_context # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: 清除上下文插件 # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 用于清除模型上下文、管理会话状态的插件。 # 插件简短描述
version: v1.0.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: tniay# 作者
repo: https://github.com/tniany/plugin_upload_clear_context #插件的仓库地址
